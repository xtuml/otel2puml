@startuml
    partition "Branch_Counts" {
        group "Branch_Counts"
            :A;
            if (XOR) then (true)
                :B;
            else (false)
                :C;
            endif
            :D;
        end group
    }
@enduml